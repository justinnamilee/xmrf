#!/bin/sh

cp .github/public/README.header.md README.md
chmod 644 README.md
pod2markdown bin/xmrf >> README.md

[ -z "$1" ] || exit 0

git diff --quiet README.md || git commit README.md -m 'doc: regenerate `README.md`'
